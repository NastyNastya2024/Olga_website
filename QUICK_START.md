# 🚀 Быстрый старт

## Проблема: "Cannot GET /admin/videos" при обновлении страницы

**Причина:** Вы используете простой HTTP-сервер (Live Server, Python http.server), который не поддерживает SPA fallback.

## ✅ Решение: Запустите Express сервер

### Шаг 1: Установите зависимости

```bash
cd backend
npm install
```

Если возникла ошибка с правами доступа, попробуйте:
```bash
sudo npm install
```

### Шаг 2: Запустите dev-сервер

```bash
node dev-server.js
```

Или используйте скрипт:
```bash
npm run dev:server
```

### Шаг 3: Откройте админку

Откройте в браузере: **http://localhost:3000/admin/videos**

---

## ❌ Почему Live Server не работает?

Live Server (VS Code) просто раздает файлы "как есть":
- Запрос `/admin/videos` → ищет файл `videos.html` → не находит → ошибка ❌

Express сервер с fallback:
- Запрос `/admin/videos` → возвращает `index.html` → JS обрабатывает маршрут ✅

---

## 🔍 Как проверить, что сервер запущен?

Откройте в браузере:
- Dev-сервер: http://localhost:3000/admin
- Основной сервер: http://localhost:5000/admin

Должна загрузиться админ-панель.

---

## 📝 Что делать дальше?

1. **Остановите Live Server** (если запущен)
2. **Установите зависимости:** `cd backend && npm install`
3. **Запустите Express сервер:** `node dev-server.js`
4. **Откройте админку** через Express сервер: `http://localhost:3000/admin`
5. **Обновите страницу (F5)** → должно работать!

---

## ⚠️ Важно

Express сервер должен быть запущен постоянно, пока вы работаете с админкой.

Если вы его остановите (Ctrl+C), обновление страницы снова перестанет работать.

---

## 🐛 Если не работает

### Ошибка "Cannot find module 'express'"

```bash
cd backend
npm install
```

### Ошибка "EACCES" или "EPERM"

```bash
sudo npm install
```

Или исправьте права доступа:
```bash
sudo chown -R $(whoami) ~/.npm
```

### Порт уже занят

Измените порт в `backend/dev-server.js`:
```javascript
const PORT = process.env.DEV_PORT || 3001; // Измените на другой порт
```
