# 📹 Рекомендации по хранению видео для сайта

## ❌ НЕ храните видео в Git

### Проблемы хранения видео в Git:
- **Размер репозитория**: Видео файлы очень большие (десятки-сотни МБ)
- **Медленная синхронизация**: Клонирование и обновление репозитория занимает много времени
- **История изменений**: Git не предназначен для бинарных файлов
- **Лимиты GitHub/GitLab**: Большие файлы могут вызвать проблемы
- **Неэффективно**: Каждое изменение видео создает новую версию в истории

## ✅ Храните видео в S3 (MinIO)

### Преимущества S3:
- ✅ **Масштабируемость**: Неограниченное хранилище
- ✅ **Производительность**: Быстрая загрузка через CDN
- ✅ **Экономия**: Не нагружает ваш сервер
- ✅ **Управление**: Легко загружать/удалять через админку
- ✅ **Версионирование**: Можно хранить разные версии с разными именами

## 📁 Рекомендуемая структура в S3

```
olga-media/
├── hero/              # Фоновые видео для главной страницы
│   └── main.mp4
├── videos/            # Видео контента (уроки, туры)
│   ├── lesson-1.mp4
│   └── lesson-2.mp4
├── images/            # Изображения
│   ├── covers/
│   └── thumbnails/
└── uploads/           # Временные загрузки
```

## 🚀 Как использовать

### 1. Загрузка фонового видео для главной страницы

```bash
# Загрузить видео в S3
node scripts/upload-hero-video.js video/main.mp4

# Скрипт вернет URL, который нужно использовать в коде
```

### 2. Использование в коде

**public/index.html:**
```html
<video id="heroVideo" autoplay muted loop playsinline preload="auto">
    <source src="http://localhost:9000/olga-media/hero/1234567890-main.mp4" type="video/mp4">
</video>
```

### 3. Загрузка через админку

В админ-панели уже есть функционал загрузки видео:
- Раздел "Видео" → "Добавить видео"
- Выберите файл → автоматически загрузится в S3
- URL автоматически заполнится в форме

## 📝 Что хранить в Git

### ✅ Хранить в Git:
- Код приложения (HTML, CSS, JS)
- Конфигурационные файлы
- Скрипты для загрузки в S3
- Документация

### ❌ НЕ хранить в Git:
- Видео файлы (*.mp4, *.mov, *.avi)
- Большие изображения (>1MB)
- Медиа контент

## 🔧 Настройка .gitignore

Убедитесь, что в `.gitignore` есть:

```
# Видео файлы
video/
*.mp4
*.mov
*.avi
*.mkv
*.webm
```

## 💡 Рекомендации по оптимизации

### 1. Оптимизация видео перед загрузкой
- **Сжатие**: Используйте HandBrake или FFmpeg для сжатия
- **Формат**: MP4 с H.264 кодеком (лучшая совместимость)
- **Разрешение**: Для фонового видео достаточно 1080p или меньше
- **Битрейт**: 2-5 Mbps для фонового видео

### 2. Пример оптимизации через FFmpeg:

```bash
# Сжать видео для веба
ffmpeg -i input.mp4 -c:v libx264 -preset slow -crf 23 -c:a aac -b:a 128k output.mp4

# Для фонового видео (меньший размер)
ffmpeg -i input.mp4 -c:v libx264 -preset slow -crf 28 -vf "scale=1920:1080" -c:a aac -b:a 96k hero.mp4
```

### 3. Использование нескольких источников

Для лучшей совместимости можно использовать несколько форматов:

```html
<video autoplay muted loop>
    <source src="video.webm" type="video/webm">
    <source src="video.mp4" type="video/mp4">
    Ваш браузер не поддерживает видео.
</video>
```

## 🔄 Миграция существующих видео

Если у вас уже есть видео в папке `video/`:

1. **Загрузите в S3:**
   ```bash
   node scripts/upload-hero-video.js video/main.mp4
   ```

2. **Обновите код:**
   - Замените локальные пути на S3 URL
   - Удалите папку `video/` из Git (но оставьте локально для разработки)

3. **Добавьте в .gitignore:**
   ```
   video/
   *.mp4
   ```

## 📊 Сравнение подходов

| Критерий | Git | S3 |
|----------|-----|-----|
| Размер репозитория | ❌ Огромный | ✅ Не влияет |
| Скорость клонирования | ❌ Медленно | ✅ Быстро |
| Управление версиями | ❌ Плохо | ✅ Хорошо |
| Производительность | ❌ Медленно | ✅ Быстро (CDN) |
| Масштабируемость | ❌ Ограничено | ✅ Неограничено |
| Стоимость | ✅ Бесплатно | ✅ Дешево/бесплатно (MinIO) |

## 🎯 Итоговые рекомендации

1. **Все видео → S3** (через админку или скрипты)
2. **Git → только код и конфигурация**
3. **Оптимизируйте видео** перед загрузкой
4. **Используйте CDN** для продакшена (CloudFront, Cloudflare)
5. **Кэшируйте видео** на клиенте (preload="auto")

## 📚 Дополнительные ресурсы

- [AWS S3 Best Practices](https://docs.aws.amazon.com/AmazonS3/latest/userguide/best-practices.html)
- [Video Optimization Guide](https://web.dev/fast/#optimize-your-images-and-video)
- [FFmpeg Documentation](https://ffmpeg.org/documentation.html)
