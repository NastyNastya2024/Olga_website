# 🚀 Запуск бэкенда

Полное руководство по запуску бэкенд-сервера для проекта Olga Website.

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Установка зависимостей](#установка-зависимостей)
3. [Запуск S3 (MinIO)](#запуск-s3-minio)
4. [Запуск бэкенд-сервера](#запуск-бэкенд-сервера)
5. [Проверка работоспособности](#проверка-работоспособности)
6. [Решение проблем](#решение-проблем)

---

## Предварительные требования

- **Node.js** версии 14 или выше
- **npm** (устанавливается вместе с Node.js)
- **Docker** и **Docker Compose** (для запуска MinIO)

Проверьте установку:

```bash
node --version
npm --version
docker --version
docker-compose --version
```

---

## Установка зависимостей

Перейдите в папку `backend` и установите зависимости:

```bash
cd backend
npm install
```

Если возникла ошибка с правами доступа:

```bash
sudo npm install
```

Или исправьте права доступа:

```bash
sudo chown -R $(whoami) ~/.npm
```

---

## Запуск S3 (MinIO)

Бэкенд использует MinIO (S3-совместимое хранилище) для хранения файлов. MinIO должен быть запущен перед запуском бэкенда.

### Шаг 1: Запустите Docker Desktop

Убедитесь, что Docker Desktop запущен на вашем компьютере.

### Шаг 2: Запустите MinIO через Docker Compose

Из корневой папки проекта:

```bash
docker-compose up -d minio
```

Или запустите все сервисы (MinIO + PostgreSQL):

```bash
docker-compose up -d
```

### Шаг 3: Проверьте статус MinIO

```bash
docker ps | grep minio
```

Должен быть запущен контейнер `olga_minio`.

### Доступ к MinIO

- **S3 API:** http://localhost:9000
- **MinIO Console (веб-интерфейс):** http://localhost:9001
- **Логин:** `minioadmin`
- **Пароль:** `minioadmin`

В MinIO Console вы можете просматривать загруженные файлы и управлять bucket'ами.

---

## Запуск бэкенд-сервера

В проекте есть два способа запуска сервера:

### Вариант 1: Dev-сервер (рекомендуется для разработки)

Dev-сервер запускается на порту **3000** и предназначен для разработки фронтенда.

```bash
cd backend
npm run dev:server
```

Или используйте скрипт из корня проекта:

```bash
./START_SERVER.sh
```

Или напрямую:

```bash
cd backend
node dev-server.js
```

**Доступ:**
- Админ-панель: http://localhost:3000/admin
- Публичный сайт: http://localhost:3000
- API: http://localhost:3000/api/...

### Вариант 2: Основной сервер (для продакшена)

Основной сервер запускается на порту **5000**.

```bash
cd backend
npm start
```

Или напрямую:

```bash
cd backend
node index.js
```

**Доступ:**
- Админ-панель: http://localhost:5000/admin
- Публичный сайт: http://localhost:5000
- API: http://localhost:5000/api/...

### Вариант 3: С автоперезагрузкой (nodemon)

Для автоматической перезагрузки при изменении файлов:

```bash
cd backend
npm run dev
```

Это запустит основной сервер с nodemon на порту 5000.

---

## Проверка работоспособности

### 1. Проверка сервера

Откройте в браузере:

- **Health check:** http://localhost:3000/health (для dev-сервера)
- **Health check:** http://localhost:5000/health (для основного сервера)

Должен вернуться JSON:

```json
{
  "status": "ok",
  "message": "Server is running"
}
```

### 2. Проверка админ-панели

Откройте в браузере:

- http://localhost:3000/admin (dev-сервер)
- http://localhost:5000/admin (основной сервер)

Должна загрузиться страница входа в админ-панель.

### 3. Проверка API

```bash
curl http://localhost:3000/api/public/videos
```

Или для основного сервера:

```bash
curl http://localhost:5000/api/public/videos
```

Должен вернуться список видео (может быть пустым массивом `[]`).

### 4. Проверка S3 подключения

Проверьте логи сервера при запуске. Должна быть строка:

```
📁 S3 endpoint: http://localhost:9000
```

---

## Решение проблем

### ❌ Ошибка: "Cannot find module 'express'"

**Причина:** Зависимости не установлены.

**Решение:**

```bash
cd backend
npm install
```

---

### ❌ Ошибка: "ENOENT: no such file or directory, open 'package.json'"

**Причина:** Команда запущена не из папки `backend`.

**Решение:**

```bash
cd backend
npm run dev:server
```

Или из корня проекта:

```bash
cd backend && npm run dev:server
```

---

### ❌ Ошибка: "EACCES" или "EPERM"

**Причина:** Проблемы с правами доступа к npm.

**Решение:**

```bash
sudo npm install
```

Или исправьте права:

```bash
sudo chown -R $(whoami) ~/.npm
```

---

### ❌ Ошибка: "Port 3000 is already in use"

**Причина:** Порт уже занят другим процессом.

**Решение 1:** Найдите и остановите процесс:

```bash
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# Или найдите процесс
lsof -i :3000
```

**Решение 2:** Измените порт в `backend/dev-server.js`:

```javascript
const PORT = process.env.DEV_PORT || 3001; // Измените на другой порт
```

Или установите переменную окружения:

```bash
DEV_PORT=3001 npm run dev:server
```

---

### ❌ Ошибка: "Cannot connect to Docker daemon"

**Причина:** Docker Desktop не запущен.

**Решение:**

1. Запустите Docker Desktop
2. Дождитесь полной загрузки (иконка Docker в трее должна быть активна)
3. Попробуйте снова:

```bash
docker-compose up -d minio
```

---

### ❌ Ошибка: "MinIO connection failed" или ошибки при загрузке файлов

**Причина:** MinIO не запущен или недоступен.

**Решение:**

1. Проверьте статус MinIO:

```bash
docker ps | grep minio
```

2. Если контейнер не запущен, запустите его:

```bash
docker-compose up -d minio
```

3. Проверьте логи MinIO:

```bash
docker logs olga_minio
```

4. Проверьте доступность MinIO:

```bash
curl http://localhost:9000/minio/health/live
```

Должен вернуться статус `200 OK`.

---

### ❌ Ошибка: "Bucket 'olga-media' does not exist"

**Причина:** Bucket не был создан при первом запуске.

**Решение:**

1. Откройте MinIO Console: http://localhost:9001
2. Войдите с учетными данными: `minioadmin` / `minioadmin`
3. Создайте bucket с именем `olga-media`
4. Установите политику доступа: `Download` (для публичного доступа)

Или создайте через Docker:

```bash
docker exec olga_minio mc mb myminio/olga-media --ignore-existing
docker exec olga_minio mc anonymous set download myminio/olga-media
```

---

### ❌ Сервер запускается, но админ-панель не работает

**Причина:** Возможно, используется неправильный URL или сервер не настроен для SPA.

**Решение:**

1. Убедитесь, что используете правильный URL:
   - ✅ http://localhost:3000/admin
   - ✅ http://localhost:3000/admin/videos
   - ❌ http://localhost:3000/admin/videos.html (неправильно)

2. Проверьте, что сервер запущен и отвечает:

```bash
curl http://localhost:3000/health
```

3. Проверьте консоль браузера (F12) на наличие ошибок JavaScript.

---

## 🔧 Настройка переменных окружения

Создайте файл `.env` в папке `backend` для настройки параметров:

```env
# Порт сервера
PORT=5000
DEV_PORT=3000

# S3 (MinIO) настройки
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=olga-media
S3_REGION=us-east-1

# JWT секрет (для аутентификации)
JWT_SECRET=your-secret-key-here
```

---

## 📝 Полезные команды

### Остановка сервера

Нажмите `Ctrl+C` в терминале, где запущен сервер.

### Остановка MinIO

```bash
docker-compose stop minio
```

Или остановите все сервисы:

```bash
docker-compose down
```

### Просмотр логов MinIO

```bash
docker logs olga_minio
```

### Просмотр логов в реальном времени

```bash
docker logs -f olga_minio
```

### Перезапуск MinIO

```bash
docker-compose restart minio
```

---

## 🎯 Рекомендации

1. **Для разработки:** Используйте `npm run dev:server` (dev-сервер на порту 3000)
2. **Для продакшена:** Используйте `npm start` (основной сервер на порту 5000)
3. **Всегда запускайте MinIO перед запуском бэкенда**
4. **Проверяйте логи при возникновении проблем**

---

## 📚 Дополнительная документация

- [README_S3.md](./README_S3.md) - Подробная информация о работе с S3
- [SPA_SERVER_SETUP.md](./SPA_SERVER_SETUP.md) - Настройка сервера для SPA
- [QUICK_START.md](../QUICK_START.md) - Быстрый старт проекта
